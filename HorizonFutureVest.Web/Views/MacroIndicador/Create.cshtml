@model HorizonFutureVest.ViewModels.MacroIndicadorViewModel
@{
    ViewData["Title"] = "Crear Macroindicador";
    var pesoDisponible = 1.0m - Model.SumaPesosActual;
}

<div class="row">
    <div class="col-md-6 offset-md-3">
        <h2 class="mb-4"><i class="bi bi-plus-circle"></i> Crear Nuevo Macroindicador</h2>

        <div class="alert alert-info">
            <i class="bi bi-info-circle"></i>
            Peso disponible: <strong>@pesoDisponible.ToString("P2")</strong>
        </div>

        <div class="card">
            <div class="card-body">
                <form asp-action="Create" method="post">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                    <div class="mb-3">
                        <label asp-for="Nombre" class="form-label"></label>
                        <input asp-for="Nombre" class="form-control" placeholder="Ej: PIB" />
                        <span asp-validation-for="Nombre" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Peso" class="form-label"></label>
                        <input asp-for="Peso" class="form-control" type="number" step="0.0001" min="0.0001" max="@pesoDisponible" />
                        <span asp-validation-for="Peso" class="text-danger"></span>
                        <small class="form-text text-muted">Valor entre 0.0001 y @pesoDisponible.ToString("F4")</small>
                    </div>

                    <div class="mb-3">
                        <div class="form-check">
                            <input asp-for="EsMejorMasAlto" class="form-check-input" type="checkbox" />
                            <label asp-for="EsMejorMasAlto" class="form-check-label"></label>
                        </div>
                        <small class="form-text text-muted">
                            Marque si valores más altos son mejores (ej: PIB). Desmarque si valores más bajos son mejores (ej: Inflación).
                        </small>
                    </div>

                    <div class="d-grid gap-2 d-md-flex">
                        <a asp-action="Index" class="btn btn-secondary">
                            <i class="bi bi-arrow-left"></i> Volver
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-save"></i> Crear Macroindicador
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
